# Copy this file to .env (or .env.production) and fill in real values.

# Backend API origin. Leave unset for local dev (falls back to
# http://localhost:3001). Set to your Render backend URL for the Vercel
# deployment, e.g. https://financehub-api.onrender.com
VITE_API_URL=
